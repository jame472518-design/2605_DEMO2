{
  "id": "sensor-bridge",
  "activation": { "onStartup": true },
  "name": "Sensor Bridge",
  "description": "Ingest sensor frames from Python bridge, broadcast SSE, run rule engine, dispatch judge agent + actuator commands, serve dashboard SPA.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  },
  "_runtimeConfigNote": "Runtime knobs are read from env vars (OpenClaw 2026.5.6's core schema rejects unknown plugins.entries.<id>.* keys before plugin manifests load, so plugin-defined config is decorative). See README for the full list."
}
